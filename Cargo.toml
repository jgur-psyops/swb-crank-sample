[package]
name = "swbcranksample"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

solana-sdk = "1.18"
solana-client = "1.18"

# switchboard-on-demand = "0.5.5"
switchboard-on-demand-client = { version = "0.3.2" }